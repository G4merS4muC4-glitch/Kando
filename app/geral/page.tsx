"use client";

import QuadroGeral from "@/components/QuadroGeral";

/** Rota do Quadro Geral (o conteudo vive em components/QuadroGeral, reaproveitado
 *  tambem pela alternancia dentro do Painel). */
export default function Geral() {
  return <QuadroGeral />;
}
