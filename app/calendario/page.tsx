import Calendario from "@/components/Calendario";

/** Pagina do calendario geral (todos os conteudos de todas as campanhas). */
export default function PaginaCalendario() {
  return <Calendario />;
}
